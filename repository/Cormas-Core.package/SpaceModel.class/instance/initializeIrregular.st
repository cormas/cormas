private - init
initializeIrregular
	
	gridCellShape := #irregular.
	nbNeighbours := #irregular.
	boundaries := #closed.
	line := nil.
	column := nil.
	cardinal := 0	"arrayOrigins := Array new: cardinal."